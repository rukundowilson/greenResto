{
  "name": "greenresto",
  "version": "1.0.0",
  "description": "Green Resto - Dessert Cart Application",
  "main": "index.js",
  "scripts": {
    "build": "tsc && rm -rf public && mkdir -p public && bash -lc '\n  cp -r index.html public/ || true\n  [ -d assets ] && cp -r assets public/ || true\n  [ -d styles ] && cp -r styles public/ || true\n  [ -d images ] && cp -r images public/ || true\n  cp -r dist/* public/ || true\n'",
    "dev": "tsc --watch",
    "serve": "npx http-server . -p 8080"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.5"
  }
}







